# Paouvir API

## 📦 Como rodar o projeto

Este projeto utiliza **Docker Compose** para subir o banco de dados MySQL.

---

## 🐳 Subindo o banco de dados

Antes de rodar a aplicação, inicie o MySQL com:

```bash
docker compose up -d